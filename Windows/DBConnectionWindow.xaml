<Window x:Class="PhoneBook.Windows.DBConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoneBook.Windows"
        mc:Ignorable="d"
        Title="DBConnectionWindow" Height="220" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Label>Подключение к БД</Label>
        <StackPanel Grid.Row="1">
            <TextBlock Grid.Row="1" TextWrapping="Wrap" Padding="0,0,0,2"
                   Style="{DynamicResource OrdinaryTextBlock}" Text="Пожалуйста, введите полную строку подключения к БД, для проверки возможности подключиться"/>
            <TextBox Grid.Row="2" Style="{DynamicResource OrdinaryTextBox}"
                     Text="{Binding DBConnectionString}"/>
        </StackPanel>
        <Button Grid.Row="2" Width="80" Height="40" 
                HorizontalAlignment="Left"
                Command="{Binding ConfirmAndCloseCommand}">Ок</Button>
    </Grid>
</Window>
