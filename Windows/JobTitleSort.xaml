<Window x:Class="PhoneBook.Windows.JobTitleSort"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhoneBook.Windows"
        mc:Ignorable="d"
        Title="JobSorting" MinHeight="650" MaxHeight="650" MinWidth="880" MaxWidth="880">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Width="120" Height="40" Margin="0,0,6,6"
                    Command="{Binding UpdateJobTitlesCommand}">Добавить должности</Button>
            <Button Width="80" Height="40" Margin="0,0,0,6"
                    Command="{Binding SaveChangesCommand}">Сохранить</Button>
        </StackPanel>
        <ui:ListView Grid.Row="1"
            ItemsSource="{Binding JobTitlesView, UpdateSourceTrigger=PropertyChanged}">
            <ui:ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70*"/>
                            <ColumnDefinition Width="30*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" Padding="0,0,6,0"
                                   Style="{DynamicResource OrdinaryTextBlock}"
                                   VerticalAlignment="Center"/>
                        <ui:NumberBox Grid.Column="1"
                            Value="{Binding Weight, Mode=TwoWay}"
                                SpinButtonPlacementMode="Inline" 
                                      VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>
        <Button Grid.Row="2" Width="80" Height="40" Margin="0,6,0,0" HorizontalAlignment="Right"
                Command="{Binding GoBackCommand}">Назад</Button>
    </Grid>
</Window>
